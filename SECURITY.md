# 安全策略

## 支持范围

当前仓库默认只维护主分支上的最新代码。部署方需要自行替换全部示例密钥和密码，并按照 [`documents/17-生产上线安全加固指南.md`](documents/17-生产上线安全加固指南.md) 完成生产配置。

## 报告安全漏洞

请不要通过公开 Issue 披露可利用的漏洞、真实凭据或用户数据。优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告入口；如果该入口尚未启用，请联系仓库维护者并仅发送最小化、已脱敏的信息。

报告建议包含：

- 受影响的模块、接口和版本或提交号；
- 可重复的验证步骤与必要请求样例；
- 影响范围和攻击前提；
- 已进行脱敏的日志、截图或概念验证；
- 建议的修复方向（如有）。

维护者确认问题前，请不要公开利用代码。不要在未经授权的服务器、账号或数据上进行测试。

## 部署方责任

- 生产环境必须使用独立强随机值配置 JWT、文件签名和 CRDT 管理密钥。
- 数据库、Redis、SMTP、对象存储和证书凭据只能通过服务器私有配置或密钥管理系统提供。
- 必须启用 HTTPS、Secure/HttpOnly/SameSite Cookie、安全响应头、访问限流、备份和监控告警。
- 文件上传应接入病毒扫描；管理端口、数据库和 Redis 不应直接暴露公网。
- 上线前应完成依赖扫描、密钥扫描、代码扫描和隔离测试环境中的渗透测试。
